<!DOCTYPE html>
<html> {% if pageName %} <title>Glow to Go || {{ pageName }}</title>{% else %} <title>Glow to Go || Default </title>{% endif %}
  <link rel="icon" href="images/logo.jpeg">
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
  <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,700" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous"> {% if page.filePathStem | calculateFileDepth | url == "." %}
  <link rel="stylesheet" href="css/bootstrap.css">
  <link rel="stylesheet" href="css/animate.css">
  <link rel="stylesheet" href="css/owl.carousel.min.css">
  <link rel="stylesheet" href="css/magnific-popup.css">
  <link rel="stylesheet" href="css/style.css"> {% else %}
  <link rel="stylesheet" href="{{ page.filePathStem | calculateFileDepth | url }}css/bootstrap.css">
  <link rel="stylesheet" href="{{ page.filePathStem | calculateFileDepth | url }}css/animate.css">
  <link rel="stylesheet" href="{{ page.filePathStem | calculateFileDepth | url }}css/owl.carousel.min.css">
  <link rel="stylesheet" href="{{ page.filePathStem | calculateFileDepth | url }}css/magnific-popup.css">
  <link rel="stylesheet" href="{{ page.filePathStem | calculateFileDepth | url }}css/style.css"> {% endif %} <header role="banner">
    <nav class="navbar navbar-expand-md navbar-dark bg-dark">
      <div class="container">
        <a class="navbar-brand" href="{{ page.filePathStem | calculateFileDepth | url }}index.html">Glow to Go</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample05" aria-controls="navbarsExample05" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="navbar-collapse navbar-light collapse show" id="navbarsExample05">
          <ul class="navbar-nav ml-auto pl-lg-5 pl-0"> {% set navPages = collections.all | eleventyNavigation %} {% macro renderNavListItem(entry) -%} {% if entry.children.length > 0 %} {%- set counter=0 %} <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="{{ entry.url | url }}" id="dropdown04" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">{{ entry.title }}</a>
              <div class="dropdown-menu" aria-labelledby="dropdown04"> {%- for child in entry.children %} {%- set counter=counter+1 %} {% if counter < 6 %} <a class="dropdown-item" href="{{ child.url | url }}">{{ child.title }}</a> {% elif counter == 6 %} {% set bloghome = collections.all | eleventyNavigation("blog") %} <a class="dropdown-item" href="{{ '/bloghome/page-1/index.html' | url }}">Click to view more blog posts</a> {% endif %} {% endfor -%} </div>
            </li> {% else %} <li class="nav-item">
              <a class="nav-link active" href="{{ entry.url | url }}">{{ entry.title }}</a>
            </li> {% endif %} {%- endmacro %} {%- for entry in navPages %} {{ renderNavListItem(entry) }} {%- endfor -%} </ul>
        </div>
      </div>
    </nav>
  </header>
  <body>